# Write your MySQL query statement below
select distinct t.email
from (select 
		email,
        row_number() over(partition by email order by email)as repitition
	  from Person) as t
where repitition > 1;
