class Solution:
    def titleToNumber(self, columnTitle: str) -> int:
        r = 0
        for ch in columnTitle:
            value = ord(ch) - ord('A') + 1
            r = r * 26 + value
        return r
        
